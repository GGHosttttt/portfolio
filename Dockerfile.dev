# Use full Node image for better dev tools support
FROM node:lts-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including devDependencies)
RUN npm ci

# Copy source code
COPY . .

# Expose Vite default dev server port
EXPOSE 5173

# Start Vite dev server with host binding (accessible from outside container)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]